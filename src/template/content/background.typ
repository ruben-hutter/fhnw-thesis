= Background <background>

This chapter introduces the foundational concepts that the remainder of the
thesis builds upon. It is intentionally short -- replace it with the relevant
theory for your own work.

== Typesetting with Typst

Typst is a modern, markup-based typesetting system designed as a fast and
approachable alternative to LaTeX. It compiles documents in milliseconds,
provides a clean scripting language, and ships with a growing package ecosystem.

== Document Structure

A thesis typically consists of front matter (title page, abstract,
acknowledgments, table of contents), the main chapters, and back matter
(bibliography and appendices). This template handles all of these automatically
once you pass your content to the `thesis` function.

See @intro for the introduction and @methodology for the methodology that
follows.
